public class Queue
{
private int [] object;
private int front;
private int QueueSize;
public QueueSize()
{
 QueueSize=3;
 Object=new int [queueSize];
front=-1;
rear=-1;
}
public void push(int data)
{
if((front+1)>=QueueSize)
reSize();
c[++front]=data;
}
public integer pop()
{
if(front>rear)
return Object[++rear];
return null;
}
public boolean is empty()
}
return front<=rear;
}
private void reSize()
{
int[] temp =object;
QueueSize=zQueueSize=2;
Object=new int =[=QueueSize];
front(int i=0; i<=front; i++)
Object[i]=temp[i];

//Then creat a driver classs

public class MyQueue
{
public static void main(String[]args)
}
Queue Queue=new Queue();
Queue.push(11);
Queue.push(13);
Queue.push(7);
system.out.println(Queue.pop());
system.out.println(Queue.pop());
system.out.println(Queue.pop());
system.out.println(Queue.pop());
system.out.println(Queue.pop());
system.out.println(Queue.pop());

}
}















